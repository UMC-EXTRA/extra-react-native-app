import { Text, View } from 'react-native';

const ChatScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Chat Page</Text>
    </View>
  );
};

export default ChatScreen;
