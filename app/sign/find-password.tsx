import { Text, View } from 'react-native';
import { BackHeaderContainer } from '@/components/Container';

// find id
const FindPassWordSceen = () => {
  return (
    <BackHeaderContainer title="비밀번호 찾기">
      <Text>Find PW</Text>
    </BackHeaderContainer>
  );
};

export default FindPassWordSceen;
